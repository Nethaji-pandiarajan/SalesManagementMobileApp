/**
 * API Configuration
 * Change the BASE_URL here to update the backend address across the entire app.
 */

const CONFIG = {
  API_BASE_URL: 'https://salesmanagementmobileapp-backend.onrender.com',
  TIMEOUT: 10000,
};

export default CONFIG;
